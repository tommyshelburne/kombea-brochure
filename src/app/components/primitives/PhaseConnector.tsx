export function PhaseConnector() {
  return (
    <div
      className="w-24 h-0.5 mt-16 flex-shrink-0"
      style={{
        background: 'linear-gradient(90deg, var(--kb-accent-blue) 0%, var(--kb-accent-purple) 100%)',
        opacity: 0.3,
      }}
    />
  );
}
